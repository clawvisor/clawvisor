---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "clawvisor_agent Resource - clawvisor"
subcategory: ""
description: |-
  A Clawvisor agent identity and its bearer token. Because the server has no agent update endpoint, changing name or description forces replacement. The sanctioned way to enroll a CI/service identity: declare a clawvisor_agent and expose its token via a sensitive output.
---

# clawvisor_agent (Resource)

A Clawvisor agent identity and its bearer token. Because the server has no agent update endpoint, changing `name` or `description` forces replacement. The sanctioned way to enroll a CI/service identity: declare a `clawvisor_agent` and expose its `token` via a sensitive output.

## Example Usage

```terraform
# A Clawvisor agent identity and its bearer token. The sanctioned way to enroll
# a CI/service identity: declare the agent and expose its token via a sensitive
# output. Changing name/description forces replacement (no server update
# endpoint); change rotate_trigger to rotate the token in place.
resource "clawvisor_agent" "ci" {
  name        = "github-actions"
  description = "CI runner service identity"
}

output "ci_agent_token" {
  value     = clawvisor_agent.ci.token # cvis_ agent token
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable agent name. Changing it forces replacement (no update endpoint).

### Optional

- `description` (String) Optional description. Changing it forces replacement (no update endpoint).
- `rotate_trigger` (String) Change this arbitrary value to rotate the agent token in place (preserves the agent id and tasks). Any new value triggers a rotate on the next apply.

### Read-Only

- `id` (String) Server-assigned agent id.
- `token` (String, Sensitive) The agent bearer token (`cvis_`). Returned only at create/rotate and stored in state; treat state as sensitive.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Agents import by server-assigned id. The token is create/rotate-only and
# cannot be recovered on import.
terraform import clawvisor_agent.ci agt_abc123
```
