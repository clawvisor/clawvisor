---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "clawvisor_content_policy Resource - clawvisor"
subcategory: ""
description: |-
  A content-scanning rule. block rules stop the request with block_message; flag rules accumulate the rule name for observation. Requires the local_governance capability (spec 06a) on OSS.
---

# clawvisor_content_policy (Resource)

A content-scanning rule. `block` rules stop the request with `block_message`; `flag` rules accumulate the rule name for observation. Requires the `local_governance` capability (spec 06a) on OSS.

## Example Usage

```terraform
# A content-scanning rule. "block" rules stop the request with block_message;
# "flag" rules accumulate the rule name for observation.
# Requires the local_governance capability (spec 06a) on OSS.
resource "clawvisor_content_policy" "block_ssn" {
  name          = "block-ssn"
  pattern       = "\\d{3}-\\d{2}-\\d{4}"
  pattern_kind  = "regex"
  action        = "block"
  block_message = "SSN-shaped content is not allowed."
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `action` (String) `block` or `flag`.
- `name` (String) Human-readable rule name (surfaced in flagged lists).
- `pattern` (String) The match pattern. Regex patterns must compile and be <=256 chars.
- `pattern_kind` (String) `regex` or `keyword` (case-insensitive substring).

### Optional

- `block_message` (String) Admin-authored message returned to the caller on a block. Defaults to empty.
- `enabled` (Boolean) Whether the rule is active. Defaults to `true`.

### Read-Only

- `id` (String) Server-assigned policy id.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import by server-assigned content-policy id.
terraform import clawvisor_content_policy.block_ssn cp_abc123
```
